<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns="http://www.w3.org/ns/widgets"
 xmlns:gap="http://phonegap.com/ns/1.0"
 id="net.pixelburn.passwordhasher"
 version="1.0.3">
    
    <name>
        PHasher+
    </name>
    
    <description>
        A simple password hasher, with HTML5 storage. Based on the original Chrome extention Password Hasher plus.
    </description>
    
    <author href="https://github.com/pixeldrew"
     email="drew@pixelburn.net">
        Drew Foehn
    </author>
    
    <gap:platforms>
        <gap:platform name="android" minVersion="2.2"/>
    </gap:platforms>
    
    <icon src="res/icon/android/icon.png" gap:role="default"/>
    <icon src="res/icon/android/icon-72-hdpi.png"   gap:platform="android"    gap:density="hdpi" />

    <feature name="http://api.phonegap.com/1.0/device" />

    <preference name="orientation"                value="default" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="false" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="android-minSdkVersion"      value="18" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

    <access origin="http://127.0.0.1*"/>


</widget>